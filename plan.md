今のドット絵エディタをマップチップ編集特化にします。

まず１ファイルは複数のマップチップとひとつのサンプルフィールドを合わせたものとします。

## マップチップ

- 各マップチップ種は接続スタイルをひとつ持つ。詳細は「接続スタイル」節を参照
- Godot(4.x) の Terrain の3種はプリセットとして用意する。ユーザー定義のスタイルも可
  - Match Corners and Sides (47枚)
  - Match Sides (16枚)
  - Match Corners (16枚)

## サンプルフィールド

- 複数のマップチップをマップチップの種類のみ区別して並べたもの
- たとえばマップチップ1,2,3があるとして:

11111
11221
13211
13111
33311

## 画面構成

### サンプルフィールド

#### サンプルフィールド編集領域

- マップチップ種類ごとに色分けされて表示される
- ユーザーが編集可能
- 一ブロックを選択可能であり、選択されているブロックに紐づくマップチップの位置に相当するドット絵をメイン編集エリアで編集可能になる

#### サンプルフィールド表示領域

- 配置されたサンプルフィールドのデータに合わせてマップチップを適切に配置したときどういう表示になるかがリアルタイムで見られる

### マップチップ

- マップチップ種の追加、削除、スタイル変更が可能

### メイン編集エリア

メインの編集エリアでは選択中のサンプルフィールドの一マスに配置されるべきマップチップの領域のひとつを編集できます



## 選択ブロックとタイルの対応

- サンプルフィールドのブロックを選ぶと、そのマスの隣接状況（上下左右・斜めの同種判定）からタイルが一意に1枚決まる。メイン編集エリアで編集するのはそのタイル
- 隣接パターンが同じマスは同じタイルを指す。1枚編集すると、同パターンの全マスに反映される
- タイル一覧から直接タイルを選んで編集するUIも用意する（サンプルフィールドに出現しないパターンも編集できるように）

## 既存機能の扱い

- 残す: カラーパレット、undo、PNG書き出し、ブラウザ自動保存
- 捨てる: 現在の「単一のドット絵」モード
- 既存の保存データとの互換は不要

## カラーパレット

- パレットはプロジェクトJSONに含める（`palette: string[]`、`#rrggbb` 小文字）
- 新規作成時は既定の20色で初期化
- スウォッチ（色見本の四角）の追加（現在の描画色を追加）・削除ができる
- パレットの一括読み込み: テキスト貼り付け欄とファイル選択（.hex / .gpl / .txt / .png）
  - テキストは形式を判別せず、6桁hex（`#` あり/なし、8桁ARGBは下6桁）と「R G B」の10進行を全部拾う
  - PNGは全ピクセルを読み、出現順に重複除去して色にする
  - 読み込み時に「置き換え」か「追加」を選べる。追加時は既存と重複する色はスキップ
- プロジェクト読込時はJSONの `palette` を表示する。無い/不正なら既定色にフォールバック

## 書き出し

出力先は Godot ではなく自作エンジン。PNG（アトラス）1ファイルだけを書き出し、対応表 JSON は PNG の `tEXt` チャンクに埋め込む。

- PNG: 全マップチップ種のタイルを1枚のアトラスにまとめる。種ごとに行を分け、各種の中は mask 昇順に左から並べる。取りうるパターンのタイルのみ含める（未使用の空きスロットは作らない）
- 埋め込み: `canvas.toBlob` で得た PNG バイト列の `IEND` チャンク直前に `tEXt` チャンクを1つ挿入する
  - keyword は `dot-map-atlas` 固定。null 区切りの後に対応表 JSON を UTF-8 で入れる（改行・インデントなしの1行）
  - チャンクは `length(4) + type(4) + data + CRC32(4)`。CRC32 は type と data に対して計算する（PNG 仕様通り）。ライブラリは使わず自前実装（30行程度）
  - 圧縮は不要（JSON は数KB）。msgpack 等の別形式にはしない
- 対応表 JSON: 各タイルが「どの接続ビットパターン」に対応するかを持つ。形式は以下

```json
{
  "format": "dot-map-atlas",
  "version": 1,
  "tileSize": 16,
  "columns": 8,
  "bitOrder": ["top", "topRight", "right", "bottomRight", "bottom", "bottomLeft", "left", "topLeft"],
  "types": [
    {
      "id": "grass",
      "name": "草地",
      "styleBits": 255,
      "tiles": [
        { "mask": 0, "x": 0, "y": 0 },
        { "mask": 1, "x": 1, "y": 0 },
        { "mask": 5, "x": 2, "y": 0 }
      ]
    }
  ]
}
```

- `bitOrder[i]` は mask の bit i が指す方向。ゲーム側はこの配列を読んで隣接判定を組む
- `styleBits` はその種が判定に使う方向の集合（8ビット）。Match Corners and Sides = 255、Match Sides = 0b01010101、Match Corners = 0b10101010
- `tiles[].x / y` はアトラス上の絶対タイル座標（ピクセルではなくタイル単位）
- mask の算出手順（ゲーム側も同じ関数を持つこと）:
  1. 8方向それぞれについて、隣が同じ種なら bit を立てる → `raw`
  2. `raw & styleBits`
  3. 角の縮約: 角ビットは、その角に隣接する2辺が両方 `styleBits` に含まれる場合、2辺とも立っているときだけ有効。そうでなければ角ビットを落とす（Godot の Match Corners and Sides と同じ規則）
  4. 結果が `mask`。`tiles` から一致するものを引く
- プロジェクトJSON（編集用）とは別物。プロジェクトJSONはピクセルデータを含み、アトラスJSONは座標だけを含む
- 検証: `test.mjs` に「書き出した PNG バイト列からチャンクを走査して `dot-map-atlas` の `tEXt` を見つけ、JSON として parse でき、CRC が一致する」テストを1つ追加する。ゲーム側の読み手も同じ手順（チャンク走査 → keyword 一致 → JSON.parse）で読む

## 接続スタイル

Godot の3種プリセットに加えて、ユーザーが独自の接続スタイルを定義できるようにする。

- スタイルは「8方向の隣接のうちどれを判定に使うか」の集合（8ビット、`styleBits`）として定義する。既存の3種はこの特殊ケースなので、内部実装は個別分岐ではなくビット集合1本に統一する
- UI: 3×3グリッド（中心を除く8マス）のチェックボックスで方向を選ぶ。プリセットボタンで3種を一発設定
  - Match Corners and Sides = 8方向すべて（47枚）
  - Match Sides = 上下左右（16枚）
  - Match Corners = 4角（16枚）
  - 例: 左右のみ → 4枚。横向きの道や柵に使う
- 角の縮約規則は「書き出し」節の mask 算出手順3と同じ。タイル数は取りうる mask の種類数から自動で求める（ハードコードしない）
- タイル一覧・書き出しには、そのスタイルで実際に取りうるパターンのみを含める
- マップチップ種の `style` は JSON に `styleBits`（数値）で保存する。プリセット名では保存しない
- スタイル変更でタイル数が変わるとき、新しいスタイルで取りえない mask のタイルは破棄する。破棄が発生する場合は確認ダイアログを出す
